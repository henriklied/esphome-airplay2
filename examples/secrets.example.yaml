# Copy to secrets.yaml next to your device config. secrets.yaml is gitignored.
wifi_ssid: "your-ssid"
wifi_password: "your-wifi-password"

# `esphome secrets` style values. Note there is no api_key here: the examples
# use a plain `api:` block, because API encryption cannot currently coexist
# with this component (see README, "Requirements").
ota_password: "your-ota-password"
